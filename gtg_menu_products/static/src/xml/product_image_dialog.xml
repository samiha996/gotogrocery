<templates xml:space="preserve">
  <t t-name="custom_products_menu.ProductImageDialog">
    <div class="position-fixed"
         style="top: 50%; left: 50%;
                transform: translate(-50%, -50%);
                z-index: 20000;
                padding: 2rem;
                background: #f5f5f5;">
      
      <!-- Close Button -->
      <button type="button"
              class="btn btn-light position-absolute"
              style="top: -10px; right: -10px; z-index: 10000;"
              t-on-click="props.close">
        ✕
      </button>

      <!-- Kanban card will be appended here -->
      <div t-ref="cardRoot"></div>
    </div>
  </t>
</templates>
